package CLASES;

import java.util.Scanner;

public class prueba {
public static void main (String[]arg) {
		
		//Creo el menu para que me pregunte si quiero empezar el programa.
		Scanner sr = new Scanner(System.in);
		System.out.println("Bien venidos al programas de Tablas de Java");
		System.out.println("*******************************************");
		System.out.println("¿Quieres que te diga alguna tabla? si/no");
				
		String res = sr.nextLine();
				
		int i = 0;				
		
		//si la contestacion es 'si' ya empieza a funcionar preguntando que tabla quiero
		while(res.equals("si")) {
			System.out.println("¿Qué tablas quieres que te muestre?");
			int x = sr.nextInt();
			while(i <= 10) {
				System.out.println(x + " x " + i +" = " + x*i);
				i++;				
			}
			//aqui tengo el problema al no conseguir que me funcione el bucle y no 
			//queria utilizar el mismo método que el anterior ejercicio.
			//pero seguiré buscando solución.
			System.out.println("¿quieres ver otra tabla?");
			//res = sr.nextLine();
			return;		
		}
			System.out.println("Esto es todo amigos");
		
		
		sr.close();		
	}

}
